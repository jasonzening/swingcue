# SwingCue Initial File Pack

This package is the minimum repo governance and execution starter for the SwingCue MVP.

## Purpose
Drop these files into the repo root at:

`C:\Users\jason\projects\swingcue`

Then commit and push before asking Claude to implement the first wave.

## First-wave target
- PR-1A: schema foundation
- PR-1B: upload + processing + deterministic analysis stub
- minimal auth/protected-route hardening only if needed for safe testability

## Suggested next commands
```bash
git add .
git commit -m "Add SwingCue repo constitution, specs, and first-wave execution docs"
git push origin main
```

## After pushing
Give Claude the prompt in:

`docs/prompts/CLAUDE_FIRST_EXECUTION_PROMPT_FINAL.md`
