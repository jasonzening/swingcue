fastapi==0.115.0
uvicorn[standard]==0.32.0
httpx==0.27.2
pydantic==2.9.2
# Heavy deps — only installed once in Docker layer.
# PR-3 hotfix v7: upgrade to numpy 2.x ecosystem. mediapipe 0.10.21 is
# the first stable version with full numpy 2.x support, with no API
# breakage from 0.10.14 (pose/face/hands solutions unchanged). opencv
# 4.10+ is already numpy-2.x-compiled, so we let pip resolve to latest.
# ultralytics 8.x is numpy-2.x native — this aligns everything.
# All three opencv variants are still pinned to the same version so that
# the trio that writes to site-packages/cv2/ produces identical bytes
# regardless of install order.
mediapipe==0.10.21
opencv-python-headless==4.10.0.84
opencv-python==4.10.0.84
opencv-contrib-python==4.10.0.84
# numpy not pinned — let mediapipe + opencv + ultralytics agree on a version
# PR-2B: fal-ai/sam-3/3d-body
fal-client==0.5.6
# PR-2B fix: removed supabase SDK (v2.7.4 rejects sb_secret_* key format,
# and the project has migrated off legacy JWT keys). We call PostgREST +
# Storage REST directly with httpx (already pinned above).
# PR-3: YOLO11m-pose via Ultralytics.
# NOTE: torch is intentionally NOT listed here. The Dockerfile installs the
# CPU-only torch wheel from pytorch.org/whl/cpu BEFORE this requirements
# file is processed, so when pip resolves `ultralytics` it sees torch is
# already satisfied and does not upgrade to the default (GPU) wheel.
# License: Ultralytics is AGPL-3.0. See docs/decisions/POSE_MODEL_LICENSE.md.
ultralytics>=8.3.0
